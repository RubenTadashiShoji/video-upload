<!-- application.html.erb -->

<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <link href="https://fonts.googleapis.com/css?family=Fjalla+One|Magra|Saira+Extra+Condensed:400,700|Staatliches&display=swap" rel="stylesheet">

  </head>
  <body>

    <div class="mobile-nav">
      <ul>
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", help_path %></li>
        <li><%= link_to "About", about_path %></li>
        <li><%= link_to "Contact", contact_path %></li>        
        <% if logged_in? %>
            <li><%= link_to "Log out", logout_path, method:"delete"%></li>
        <% else %>
            <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </div>
    <main>
      <%= render 'layouts/header' %>
      <section id="hero">
        <div class="container">
          <div class="hero-text">
            <h1 class="heading">living the moment</h1>
            <!-- <div class="separator left"></div> -->
            <h2 class="subheading">Video Competition</h2>
            <!-- <div class="separator right"></div> -->
          </div>
        </div>
      </section>
      <section id="content" style="margin-bottom: 10em;">
        <div class="container">
          <% flash.each do |message_type, message| %>
            <div class="alert alert-<%= message_type %>"><%= message %></div>
          <% end %>
          <%= yield %>
        </div>
      </section>
      <section id="footer">
        <div class="container">
          <%= render 'layouts/footer' %>
        </div>
      </section>
    </main>
  </body>
</html>


<script type="text/javascript">
  
  $('.hamburger-menu').click(function() {
    event.stopPropagation();  
    if ($('main').hasClass('active')) {
      $('main').removeClass('active');
    } else {
      $('main').addClass('active');
    }
  });
  
  $('main').click(function() {
    $('main').removeClass('active');
  });
  
</script>
